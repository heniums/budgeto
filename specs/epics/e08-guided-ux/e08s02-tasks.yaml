story_id: e08s02
title: Onboarding wizard + empty-state guidance
status: failing
bcps: 5
spec: e08s02-onboarding-wizard.md
tasks:
  - id: 1
    description: >
      Build an OnboardingWizard component using shadcn Dialog/Sheet with 3
      steps: (1) Create Wallet form, (2) Create Category form, (3) Add
      Transaction form. Each step auto-advances on success. The wizard is
      dismissable via an X button and stores a 'wizardDismissed' flag in
      localStorage so it only shows on first visit. Re-triggerable from a
      "Start setup" item in the user menu.
    verify: 'npm test -- client/src/components/OnboardingWizard.test.tsx'
    risk: P1
    status: passing
    allure:
      severity: high
      categories:
        - 'Client'
        - 'Onboarding'
  - id: 2
    description: >
      Add a shouldShowWizard() check in Home.tsx: if the authenticated user
      has zero wallets AND wizardDismissed is not set, render the
      OnboardingWizard instead of the transaction table.
    verify: 'npm test -- client/src/pages/Home.test.tsx'
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - 'Client'
        - 'Onboarding'
  - id: 3
    description: >
      Add conditional empty-state prompts to Home.tsx for returning users:
      when wallets.length === 0, show "You have no wallets yet — create your
      first wallet" with a CTA button that opens the wallet creation form.
      When wallets.length > 0 and categories.length === 0, show "You have no
      categories yet — create your first category" with a CTA. Fetch
      categories on Home mount alongside transactions and wallets.
    verify: 'npm test -- client/src/pages/Home.test.tsx'
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - 'Client'
        - 'Empty States'
  - id: 4
    description: >
      In the Add Transaction dialog, disable the submit button and show an
      inline warning when no wallets or no categories exist, with a link to
      create the missing resource (wired to the stacked modal pattern from
      e08s04). This handles the edge case where the user dismissed the
      wizard but still lacks prerequisites.
    verify: 'npm test -- client/src/components/TransactionForm.test.tsx'
    risk: P1
    status: failing
    allure:
      severity: high
      categories:
        - 'Client'
        - 'Forms'
